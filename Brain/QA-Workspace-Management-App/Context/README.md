---
title: Context — QA-Workspace-Management-App
type: index
---

# Context

Reference material Claude pulls up on demand. Not decisions (those live in `ADR/` or `Rules/`) — just **what-and-where** lookups that are larger than a rule but smaller than a spec.

## Files

- [[file-map]] — every significant file + one-line purpose

## When to update

- After a major refactor — refresh `file-map.md`.
- After a big structural change — consider whether a matching rule in `Rules/project-structure.md` also needs updating.

Keep these files **terse**. They are indexes, not tutorials. For tech-stack details and critical-path flows, see the rule files under `Rules/` (`project-structure.md`, `api-conventions.md`, `error-handling.md`).
